# Copyright 2026 FlagOS Contributors
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: Common Functional Tests - Benchmark

on:
  workflow_call:
    inputs:
      platform:
        required: true
        type: string
        description: Platform name (e.g., cuda, default)
      test_matrix:
        required: true
        type: string
        description: JSON array of test configurations
      image:
        required: true
        type: string
      runs_on:
        required: true
        type: string
      container_volumes:
        required: true
        type: string
      container_options:
        required: true
        type: string
      pkg_mgr:
        required: false
        type: string
        description: Package manager (pip, uv, conda). Default uv.
        default: "uv"
      env_name:
        required: false
        type: string
        description: Conda environment name (for conda only)
        default: ""
      env_path:
        required: false
        type: string
        description: Environment path (venv path for uv, conda installation path for conda)
        default: "/opt/venv"
      megatron_cache_key:
        required: false
        type: string
        default: ''
      megatron_artifact_available:
        required: false
        type: string
        default: 'false'
      te_fl_cache_key:
        required: false
        type: string
        default: ''
      te_fl_artifact_available:
        required: false
        type: string
        default: 'false'
      megatron_lm_fl_enabled:
        required: false
        type: string
        default: 'false'
      te_fl_enabled:
        required: false
        type: string
        default: 'false'
      megatron_runtime_environment:
        required: false
        type: string
        default: '{}'
      te_fl_runtime_environment:
        required: false
        type: string
        default: '{}'
      te_fl_install_pip_args:
        required: false
        type: string
        default: '[]'
      te_fl_runtime_pip_packages:
        required: false
        type: string
        default: '[]'

jobs:
  functional_test_benchmark:
    defaults:
      run:
        shell: bash
    env:
      PROJECT_ROOT: ${{ github.workspace }}/FlagScale
    runs-on: ${{ fromJson(inputs.runs_on) }}
    strategy:
      fail-fast: false
      matrix:
        test_config: ${{ fromJson(inputs.test_matrix) }}
    container:
      image: ${{ inputs.image }}
      ports:
        - 80
      volumes: ${{ fromJson(inputs.container_volumes) }}
      options: ${{ inputs.container_options }}

    steps:
      - name: Checkout code (attempt 1)
        id: checkout_attempt_1
        uses: actions/checkout@v4
        continue-on-error: true
        with:
          repository: ${{ github.event.pull_request.head.repo.full_name || github.repository }}
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          path: FlagScale
          ssh-strict: true
          ssh-user: git
          persist-credentials: false
          clean: true
          sparse-checkout-cone-mode: true
          fetch-tags: false
          show-progress: true
          lfs: false
          submodules: false
          set-safe-directory: true

      - name: Checkout code (attempt 2)
        if: steps.checkout_attempt_1.outcome == 'failure'
        id: checkout_attempt_2
        uses: actions/checkout@v4
        continue-on-error: true
        with:
          repository: ${{ github.event.pull_request.head.repo.full_name || github.repository }}
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          path: FlagScale
          ssh-strict: true
          ssh-user: git
          persist-credentials: false
          clean: true
          sparse-checkout-cone-mode: true
          fetch-tags: false
          show-progress: true
          lfs: false
          submodules: false
          set-safe-directory: true

      - name: Checkout code (attempt 3)
        if: steps.checkout_attempt_2.outcome == 'failure'
        id: checkout_attempt_3
        uses: actions/checkout@v4
        with:
          repository: ${{ github.event.pull_request.head.repo.full_name || github.repository }}
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          path: FlagScale
          ssh-strict: true
          ssh-user: git
          persist-credentials: false
          clean: true
          sparse-checkout-cone-mode: true
          fetch-tags: false
          show-progress: true
          lfs: false
          submodules: false
          set-safe-directory: true

      - name: Set safe directory
        run: |
          git config --global --add safe.directory $PROJECT_ROOT

      - name: Check environment info
        run: cd $PROJECT_ROOT && bash ./tests/test_utils/runners/check_env.sh

      - name: Reset prepared runtime directories
        if: inputs.megatron_lm_fl_enabled == 'true' || inputs.te_fl_enabled == 'true'
        working-directory: ${{ github.workspace }}
        run: |
          set -euo pipefail
          rm -rf megatron-lm-fl-install te-fl-wheel

      - name: Restore Megatron-LM-FL cache
        if: inputs.megatron_lm_fl_enabled == 'true' && inputs.megatron_cache_key != ''
        id: megatron_cache_restore
        uses: actions/cache/restore@v4
        with:
          path: megatron-lm-fl-install
          key: ${{ inputs.megatron_cache_key }}
          fail-on-cache-miss: false

      - name: Restore TE-FL cache
        if: inputs.te_fl_enabled == 'true' && inputs.te_fl_cache_key != ''
        id: te_fl_cache_restore
        uses: actions/cache/restore@v4
        with:
          path: te-fl-wheel
          key: ${{ inputs.te_fl_cache_key }}
          fail-on-cache-miss: false

      - name: Restore prepared Megatron artifact
        if: inputs.megatron_lm_fl_enabled == 'true' && inputs.megatron_cache_key != '' && steps.megatron_cache_restore.outputs.cache-hit != 'true' && inputs.megatron_artifact_available == 'true'
        uses: actions/download-artifact@v4
        with:
          name: prepared-megatron-${{ inputs.platform }}-${{ github.run_id }}
          path: megatron-lm-fl-install

      - name: Restore prepared TE-FL artifact
        if: inputs.te_fl_enabled == 'true' && inputs.te_fl_cache_key != '' && steps.te_fl_cache_restore.outputs.cache-hit != 'true' && inputs.te_fl_artifact_available == 'true'
        uses: actions/download-artifact@v4
        with:
          name: prepared-te-fl-${{ inputs.platform }}-${{ github.run_id }}
          path: te-fl-wheel

      - name: Validate prepared training runtime
        if: inputs.megatron_lm_fl_enabled == 'true' || inputs.te_fl_enabled == 'true'
        run: |
          set -euo pipefail
          if [ "${{ inputs.megatron_lm_fl_enabled }}" = "true" ] && [ ! -f megatron-lm-fl-install/megatron/core/__init__.py ]; then
            echo "::error::Prepared Megatron-LM-FL runtime is missing megatron/core/__init__.py" >&2
            exit 1
          fi
          if [ "${{ inputs.te_fl_enabled }}" = "true" ]; then
            mapfile -t te_fl_wheels < <(
              find te-fl-wheel -maxdepth 1 -type f -name 'transformer_engine*.whl' -print 2>/dev/null | sort
            )
            if [ "${#te_fl_wheels[@]}" -ne 1 ]; then
              echo "::error::Expected exactly one TE-FL wheel, found ${#te_fl_wheels[@]}" >&2
              printf '  %s\n' "${te_fl_wheels[@]}" >&2
              exit 1
            fi
          fi

      - name: Install prepared training runtime
        if: inputs.megatron_lm_fl_enabled == 'true' || inputs.te_fl_enabled == 'true'
        run: |
          set -euo pipefail
          cd $PROJECT_ROOT
          export MEGATRON_LM_FL_ENABLED='${{ inputs.megatron_lm_fl_enabled }}'
          export MEGATRON_CACHE_KEY='${{ inputs.megatron_cache_key }}'
          export MEGATRON_RUNTIME_ENVIRONMENT='${{ inputs.megatron_runtime_environment }}'
          export TE_FL_ENABLED='${{ inputs.te_fl_enabled }}'
          export TE_FL_CACHE_KEY='${{ inputs.te_fl_cache_key }}'
          export TE_FL_RUNTIME_ENVIRONMENT='${{ inputs.te_fl_runtime_environment }}'
          export TE_FL_INSTALL_PIP_ARGS='${{ inputs.te_fl_install_pip_args }}'
          export TE_FL_RUNTIME_PIP_PACKAGES='${{ inputs.te_fl_runtime_pip_packages }}'
          export CI_RUNTIME_PKG_MGR='${{ inputs.pkg_mgr }}'
          export CI_RUNTIME_ENV_NAME='${{ inputs.env_name }}'
          export CI_RUNTIME_ENV_PATH='${{ inputs.env_path }}'
          bash .github/scripts/install_training_runtime.sh

      - name: Install dependencies for benchmark
        run: |
          set -euo pipefail
          cd $PROJECT_ROOT

          bash ./tests/test_utils/runners/setup_training_test_env.sh \
            --platform '${{ inputs.platform }}' \
            --pkg-mgr '${{ inputs.pkg_mgr }}' \
            --env-name '${{ inputs.env_name }}' \
            --env-path '${{ inputs.env_path }}' \
            --install-cli
        timeout-minutes: 30

      - name: Run benchmark tests
        id: benchmark_test
        run: |
          set -euo pipefail
          cd $PROJECT_ROOT

          PLATFORM='${{ inputs.platform }}'
          DEVICE='${{ matrix.test_config.device }}'
          TASK='${{ matrix.test_config.task }}'
          MODEL='${{ matrix.test_config.model }}'
          CASE='${{ matrix.test_config.case }}'
          PKG_MGR='${{ inputs.pkg_mgr }}'
          ENV_NAME='${{ inputs.env_name }}'
          ENV_PATH='${{ inputs.env_path }}'

          echo "Running benchmark tests"
          echo "Platform: $PLATFORM"
          echo "Device: $DEVICE"
          echo "Task: $TASK"
          echo "Model: $MODEL"
          echo "Case: ${CASE:-all}"
          echo "Package Manager: $PKG_MGR"
          echo "Environment Name: $ENV_NAME"
          echo "Environment Path: $ENV_PATH"
          echo "Project root: $PROJECT_ROOT"

          # Source environment utilities
          source ./tools/install/utils/pyenv_utils.sh

          # Activate environment based on package manager
          case "$PKG_MGR" in
            conda)
              if [ -n "$ENV_NAME" ]; then
                activate_conda "$ENV_NAME" "$ENV_PATH" || echo "Conda activation failed"
              fi
              ;;
            uv)
              if [ -n "$ENV_PATH" ] && [ -d "$ENV_PATH" ]; then
                activate_uv_env "$ENV_PATH" || echo "UV activation failed"
              fi
              ;;
            pip)
              echo "Running tests with pip/system Python"
              ;;
          esac

          # Display the interpreter selected during environment setup.
          echo "Python location: ${CI_PYTHON_BIN:-python3}"
          echo "Python version: $(${CI_PYTHON_BIN:-python3} --version)"

          # Collect GPU info before benchmark
          echo "=========================================="
          echo "GPU Information"
          echo "=========================================="
          nvidia-smi || echo "nvidia-smi not available"
          echo "=========================================="

          # Run benchmark tests using run_tests.sh
          bash "$PROJECT_ROOT/tests/test_utils/runners/run_tests.sh" \
            --platform "$PLATFORM" \
            --device "$DEVICE" \
            --type functional \
            --task "$TASK" \
            --model "$MODEL" \
            --list "$CASE"
          exit_code=$?

          if [ $exit_code -eq 0 ]; then
            echo "Benchmark tests passed for $PLATFORM/$DEVICE/$TASK/$MODEL/$CASE"
          else
            echo "Benchmark tests failed for $PLATFORM/$DEVICE/$TASK/$MODEL/$CASE (exit code: $exit_code)"
          fi

          echo "exit_code=$exit_code" >> $GITHUB_OUTPUT
          exit $exit_code
        timeout-minutes: 60

      - name: Parse benchmark output to JSON
        if: always() && steps.benchmark_test.outcome == 'success'
        run: |
          set -euo pipefail
          cd $PROJECT_ROOT

          TASK='${{ matrix.test_config.task }}'
          MODEL='${{ matrix.test_config.model }}'
          CASE='${{ matrix.test_config.case }}'
          PKG_MGR='${{ inputs.pkg_mgr }}'
          ENV_NAME='${{ inputs.env_name }}'
          ENV_PATH='${{ inputs.env_path }}'

          source ./tools/install/utils/pyenv_utils.sh
          case "$PKG_MGR" in
            conda)
              if [ -n "$ENV_NAME" ]; then
                activate_conda "$ENV_NAME" "$ENV_PATH" || { echo "Conda activation failed"; exit 1; }
              fi
              ;;
            uv)
              if [ -n "$ENV_PATH" ] && [ -d "$ENV_PATH" ]; then
                activate_uv_env "$ENV_PATH" || { echo "UV activation failed"; exit 1; }
              fi
              ;;
            pip)
              echo "Using system Python with pip"
              ;;
            *)
              echo "Unsupported package manager: $PKG_MGR"
              exit 1
              ;;
          esac

          echo "Python location: ${CI_PYTHON_BIN:-python3}"
          echo "Python version: $(${CI_PYTHON_BIN:-python3} --version)"

          LOG_FILE="tests/functional_tests/${TASK}/${MODEL}/test_results/${CASE}/logs/host_0_localhost.output"
          GOLD_FILE="tests/functional_tests/${TASK}/${MODEL}/gold_values/${CASE}.json"
          OUTPUT_FILE="tests/functional_tests/${TASK}/${MODEL}/test_results/${CASE}/logs/benchmark_metrics.json"

          echo "Parsing benchmark output to JSON"
          echo "Log file: $LOG_FILE"
          echo "Gold values: $GOLD_FILE"
          echo "Output: $OUTPUT_FILE"

          "${CI_PYTHON_BIN:-python3}" tests/test_utils/runners/parse_benchmark_output.py \
            "$LOG_FILE" "$GOLD_FILE" "$OUTPUT_FILE"

      - name: Install jq
        if: always() && steps.benchmark_test.outcome == 'success'
        run: apt-get update && apt-get install -y jq

      - name: Upload benchmark data to backend
        if: always() && steps.benchmark_test.outcome == 'success'
        uses: flagos-ai/FlagOps/actions/post-benchmark-report@main
        env:
          NO_PROXY: "flagcicd-inner.flagos.net"
        with:
          backend_url: 'http://flagcicd-inner.flagos.net:8000'
          user_id: '000000000000000000'
          report_path: ${{ env.PROJECT_ROOT }}/tests/functional_tests/${{ matrix.test_config.task }}/${{ matrix.test_config.model }}/test_results/${{ matrix.test_config.case }}/logs/benchmark_metrics.json
          list_code: 'benchmark_${{ matrix.test_config.task }}_${{ matrix.test_config.model }}_${{ matrix.test_config.case }}'
          list_name: 'Benchmark-${{ matrix.test_config.model }}-${{ matrix.test_config.case }}'
          header_config: '[{"field":"metric","name":"Benchmark Metric","required":true,"sortable":true,"type":"string"},{"field":"values","name":"Per Step Details","required":false,"sortable":false,"type":"array"},{"field":"avg","name":"Avg","required":true,"sortable":true,"type":"number"},{"field":"p50","name":"P50","required":true,"sortable":true,"type":"number"},{"field":"p99","name":"P99","required":true,"sortable":true,"type":"number"}]'
          repository_name: '${{ github.repository }}'
          commit_id: '${{ github.event.pull_request.head.sha || github.sha }}'
          workflow_id: '${{ github.run_id }}'
          runner_set_name: ${{ fromJson(inputs.runs_on)[0] }}
          fail_on_error: 'false'

      - name: Upload Benchmark Test Logs
        if: always() && steps.benchmark_test.outcome == 'failure'
        uses: actions/upload-artifact@v4
        continue-on-error: true
        with:
          name: benchmark_tests-logs-${{ inputs.platform }}-${{ github.run_id }}-${{ matrix.test_config.task }}-${{ matrix.test_config.model }}
          path: ${{ env.PROJECT_ROOT }}/tests/functional_tests/${{ matrix.test_config.task }}/${{ matrix.test_config.model }}/test_results
          retention-days: 7
          if-no-files-found: warn
