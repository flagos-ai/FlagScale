diff --git a/hardware/Huawei_Atlas800TA3/Megatron-LM/megatron/core/transformer/transformer_layer.py.patch b/hardware/Huawei_Atlas800TA3/Megatron-LM/megatron/core/transformer/transformer_layer.py.patch
index 060c30d6..b4ffeb1c 100644
--- a/hardware/Huawei_Atlas800TA3/Megatron-LM/megatron/core/transformer/transformer_layer.py.patch
+++ b/hardware/Huawei_Atlas800TA3/Megatron-LM/megatron/core/transformer/transformer_layer.py.patch
@@ -1,36 +1,38 @@
 diff --git a/megatron/core/transformer/transformer_layer.py b/megatron/core/transformer/transformer_layer.py
-index 51c90f5ac..56212d24b 100644
+index 42d077e2e..bd22be19d 100644
 --- a/megatron/core/transformer/transformer_layer.py
 +++ b/megatron/core/transformer/transformer_layer.py
-@@ -34,7 +34,7 @@ from megatron.core.utils import (
- logger = logging.getLogger(__name__)
+@@ -36,17 +36,18 @@ logger = logging.getLogger(__name__)
  
  
--def get_transformer_layer_offset(config: TransformerConfig, vp_stage: Optional[int] = None):
-+def get_transformer_layer_offset(config: TransformerConfig, vp_stage: Optional[int] = None, is_dualpipev_first_chunk: Optional[bool] = False):
+ def get_transformer_layer_offset(
+-    config: TransformerConfig, vp_stage: Optional[int] = None, pp_rank: Optional[int] = None
+-):
++    config: TransformerConfig, vp_stage: Optional[int] = None, pp_rank: Optional[int] = None, is_dualpipev_first_chunk: Optional[bool] = False):
      """Get the index offset of current pipeline stage, given the level of pipelining."""
-     pipeline_rank = parallel_state.get_pipeline_model_parallel_rank()
-     if not parallel_state.is_inside_encoder():
-@@ -43,8 +43,9 @@ def get_transformer_layer_offset(config: TransformerConfig, vp_stage: Optional[i
-             pipeline_rank = pipeline_rank - pp_decoder_start
+     if pp_rank is None:
+         pp_rank = parallel_state.get_pipeline_model_parallel_rank()
+ 
++
+     is_first_pp_stage = pp_rank == 0
  
      if config.pipeline_model_parallel_size > 1:
 -
 -        if config.pipeline_model_parallel_layout:
 +        if config.enable_hetero:
-+            offset = sum(([0] + config.hetero_pipeline_layer_split)[: pipeline_rank + 1])
++            offset = sum(([0] + config.hetero_pipeline_layer_split)[: pp_rank + 1])
 +        elif config.pipeline_model_parallel_layout:
              offset = config.pipeline_model_parallel_layout.get_layer_offset(
                  layer_type=LayerType.decoder, vp_stage=vp_stage
              )
-@@ -143,12 +144,68 @@ def get_transformer_layer_offset(config: TransformerConfig, vp_stage: Optional[i
-                     else pipeline_rank - 1
-                 )
+@@ -143,12 +144,68 @@ def get_transformer_layer_offset(
+                 else:
+                     num_layers_per_pipeline_rank = 0
  
--                if pipeline_rank == 0:
+-                if pp_rank == 0:
 -                    offset = 0
 +                if not config.use_dualpipev:
-+                    if pipeline_rank == 0:
++                    if pp_rank == 0:
 +                        offset = 0
 +                    else:
 +                        offset = (
@@ -60,11 +62,11 @@ index 51c90f5ac..56212d24b 100644
 +                    # process first chunk
 +                    if is_dualpipev_first_chunk:
 +                        middle_pipeline_rank = (
-+                            pipeline_rank
++                            pp_rank
 +                            if config.num_layers_in_first_pipeline_stage is None
-+                            else pipeline_rank - 1
++                            else pp_rank - 1
 +                        )
-+                        if pipeline_rank == 0:
++                        if pp_rank == 0:
 +                            offset = 0
 +                        else:
 +                            offset = (
@@ -82,12 +84,12 @@ index 51c90f5ac..56212d24b 100644
 +                            start_offset = start_offset - num_layers_per_pipeline_rank_first_chunk + num_layers_in_last_pipeline_stage_first_chunk
 +
 +                        middle_pipeline_rank = (
-+                            config.pipeline_model_parallel_size - 1 - pipeline_rank
++                            config.pipeline_model_parallel_size - 1 - pp_rank
 +                            if config.num_layers_in_last_pipeline_stage is None
-+                            else config.pipeline_model_parallel_size - 1 - pipeline_rank - 1
++                            else config.pipeline_model_parallel_size - 1 - pp_rank - 1
 +                        )
 +
-+                        if pipeline_rank == config.pipeline_model_parallel_size - 1:
++                        if pp_rank == config.pipeline_model_parallel_size - 1:
 +                            offset = start_offset
 +                        else:
 +                            offset = start_offset + (
@@ -97,20 +99,23 @@ index 51c90f5ac..56212d24b 100644
          else:
              num_layers = config.num_layers
  
-@@ -182,16 +239,30 @@ def get_transformer_layer_offset(config: TransformerConfig, vp_stage: Optional[i
+@@ -180,13 +237,26 @@ def get_transformer_layer_offset(
                  ):
                      offset -= 1
              else:
--                offset = pipeline_rank * num_layers_per_pipeline_rank
+-                offset = pp_rank * num_layers_per_pipeline_rank
+-
+-                # Reduce the offset of embedding layer from the total layer number
+-                if config.account_for_embedding_in_pipeline_split and not (
+-                    is_vp_first_stage(vp_stage, vp_size) and is_first_pp_stage
+-                ):
+-                    offset -= 1
 +                if not config.use_dualpipev:
-+                    offset = pipeline_rank * num_layers_per_pipeline_rank
++                    offset = pp_rank * num_layers_per_pipeline_rank
 +
 +                    # Reduce the offset of embedding layer from the total layer number
-+                    if (
-+                        config.account_for_embedding_in_pipeline_split
-+                        and not parallel_state.is_pipeline_first_stage(
-+                            ignore_virtual=False, vp_stage=vp_stage
-+                        )
++                    if config.account_for_embedding_in_pipeline_split and not (
++                        is_vp_first_stage(vp_stage, vp_size) and is_first_pp_stage
 +                    ):
 +                        offset -= 1
 +                ######### FlagScale Begin ########
@@ -121,19 +126,10 @@ index 51c90f5ac..56212d24b 100644
 +                    num_layers_per_pipeline_rank_second_chunk = num_layers_per_pipeline_rank - num_layers_per_pipeline_rank_first_chunk
 +
 +                    if is_dualpipev_first_chunk:
-+                        offset = pipeline_rank * num_layers_per_pipeline_rank_first_chunk
++                        offset = pp_rank * num_layers_per_pipeline_rank_first_chunk
 +                    else:
-+                        offset = config.num_layers - ((pipeline_rank+1) * num_layers_per_pipeline_rank_second_chunk)
++                        offset = config.num_layers - ((pp_rank+1) * num_layers_per_pipeline_rank_second_chunk)
 +                ######### FlagScale End ########
- 
--                # Reduce the offset of embedding layer from the total layer number
--                if (
--                    config.account_for_embedding_in_pipeline_split
--                    and not parallel_state.is_pipeline_first_stage(
--                        ignore_virtual=False, vp_stage=vp_stage
--                    )
--                ):
--                    offset -= 1
      else:
          offset = 0
      return offset

