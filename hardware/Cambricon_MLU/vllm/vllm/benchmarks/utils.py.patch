diff --git a/vllm/benchmarks/lib/utils.py b/vllm/benchmarks/utils.py
similarity index 84%
rename from vllm/benchmarks/lib/utils.py
rename to vllm/benchmarks/utils.py
index 0c27687dc..f0bb99326 100644
--- a/vllm/benchmarks/lib/utils.py
+++ b/vllm/benchmarks/utils.py
@@ -54,12 +54,7 @@ class InfEncoder(json.JSONEncoder):
 
     def clear_inf(self, o: Any):
         if isinstance(o, dict):
-            return {
-                str(k)
-                if not isinstance(k, (str, int, float, bool, type(None)))
-                else k: self.clear_inf(v)
-                for k, v in o.items()
-            }
+            return {k: self.clear_inf(v) for k, v in o.items()}
         elif isinstance(o, list):
             return [self.clear_inf(v) for v in o]
         elif isinstance(o, float) and math.isinf(o):
@@ -72,9 +67,4 @@ class InfEncoder(json.JSONEncoder):
 
 def write_to_json(filename: str, records: list) -> None:
     with open(filename, "w") as f:
-        json.dump(
-            records,
-            f,
-            cls=InfEncoder,
-            default=lambda o: f"<{type(o).__name__} is not JSON serializable>",
-        )
+        json.dump(records, f, cls=InfEncoder)

